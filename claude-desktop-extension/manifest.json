{
  "manifest_version": "0.3",
  "name": "secondmind",
  "display_name": "SecondMind",
  "version": "0.1.0",
  "description": "Portable, cross-session AI memory. Zero required dependencies. One memory. Every AI. Any machine.",
  "long_description": "SecondMind is a local, zero-install memory layer: tell it something once and any MCP-compatible client (Claude Desktop, Claude Code, and others as they adopt the same standard) can recall it later, on any machine. Hybrid BM25+semantic search, conflict-safe writes, and a plain Markdown vault you fully own.",
  "author": {
    "name": "Al Amin"
  },
  "icon": "icon.png",
  "server": {
    "type": "binary",
    "entry_point": "run.sh",
    "mcp_config": {
      "command": "/bin/sh",
      "args": ["${__dirname}/run.sh"],
      "env": {
        "SECONDMIND_REPO_DIR": "${user_config.repo_dir}",
        "SECONDMIND_VAULT": "${user_config.vault_dir}",
        "SECONDMIND_INDEX_DB": "${user_config.index_db}"
      },
      "platform_overrides": {
        "win32": {
          "command": "${__dirname}/run.bat"
        }
      }
    }
  },
  "user_config": {
    "repo_dir": {
      "type": "directory",
      "title": "SecondMind repo location (required)",
      "description": "The folder where you cloned https://github.com/al-amin/SecondMind — Claude Desktop copies this extension's own files elsewhere when installing it, so the launcher can no longer find the repo on its own. Point this at your actual clone (the folder containing pyproject.toml), not the extension folder itself.",
      "required": true
    },
    "vault_dir": {
      "type": "directory",
      "title": "Vault location",
      "description": "Where SecondMind stores your notes as plain Markdown files. Defaults to a folder in your home directory — change it if you want your vault somewhere else (e.g. inside an existing Obsidian vault, or a synced folder).",
      "required": false,
      "default": "${HOME}/.secondmind/vault"
    },
    "index_db": {
      "type": "file",
      "title": "Search index location",
      "description": "Where SecondMind keeps its rebuildable search index (SQLite). Safe to delete any time — it regenerates from the vault. Kept separate from vault_dir on purpose, so a vault pointed at an existing Obsidian folder never gets a stray database file mixed into your real notes.",
      "required": false,
      "default": "${HOME}/.secondmind/index.db"
    }
  },
  "tools_generated": true,
  "keywords": ["memory", "personal", "knowledge-base", "mcp"],
  "license": "Apache-2.0",
  "compatibility": {
    "claude_desktop": ">=0.10.0",
    "platforms": ["darwin", "win32"],
    "runtimes": {}
  }
}
